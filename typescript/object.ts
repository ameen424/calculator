const person : {
    name : string;
    age : number;
    branch : string;
} = {
    name : 'ameen',
    age : 22,
    branch : 'cse'
};

console.log(person.name);
console.log(person.age);
console.log(person.branch);
